#Start Writing, How the implementation is working, the urls and APIS, etc. DOCUMENT
#Re-write the SUL functions DOCUMENT
#Re-write the Trace Generator DOCUMENT
-Re-write the new version of teacher.py with new fields added => aggregation and n_min
-Re-design the UI page for get the new fields from the user
-Connect the new teacher.py to tasks.py and run it
-Check and correct the new SUL and Trace Generator
-Test all defined case studies from zero to 100, with using teacher.py too
-See if it creates correct ressults or not
-Create the UI to show the results to the user
-Give a complete downloadable JSON file to the user from the CS which defined
-Go for CSV and make it's first form pages to seperate driversignal, etc.
#Re-write the teacher in DOCUMENT


Chapter 1: Introduction (The Ideal Structure)

1.1 Context and Motivation

    Briefly explain what Hybrid Systems and the L*SHA algorithm are (high-level).

    Explain why analyzing these systems is important (e.g., safety-critical systems like thermostats, medical devices, or autonomous drones).

    Discuss the shift toward web-based, accessible tools in academia and industry.

1.2 Problem Statement

    Describe the state of the original GitHub repository: It was a collection of rigid, hardcoded Python scripts.

    Explain the pain points: To test a new system, a researcher had to manually rewrite Python code (like the THERMO hack), deeply understand the codebase, and run everything locally via terminal. It was not scalable or user-friendly.

1.3 Aims and Objectives (Your Contributions)

    State exactly what you built: A dynamic, decoupled, web-based platform for L*SHA learning.

    Bullet points of your specific achievements:

        Designing a scalable database and JSON configuration schema.

        Building "Translator/Adapter" layers to make the core algorithm universally applicable.

        Developing a modern frontend (React) for configuration and visualization.

1.4 Scope and Future Vision

    Define the current boundaries of your work: Explicitly mention that the current trace generation is tightly integrated with UPPAAL.

    Mention the future-proofing: State that the architecture was intentionally designed using the Strategy Pattern so that alternative data ingestion methods (like CSV files) can be easily plugged in later without rewriting the core algorithm.

1.5 Document Outline

    A brief paragraph summarizing what the reader will find in Chapters 2, 3, 4, etc.
