# ── lsha_web — pinned runtime dependencies ──────────────────────────────
# Python 3.10 required (sub-packages under core_algorithm declare python = "^3.10").
# Generated from the working `lsha-web` conda env. Install into a fresh venv with:
#     python3.10 -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt

# --- Web / API layer ---
Django==5.2.7
djangorestframework==3.16.1
django-cors-headers==4.9.0
django-celery-results==2.6.0
django-filter==25.2

# --- Async task queue (needs a running Redis server) ---
celery==5.5.3
redis==7.0.1

# --- Core algorithm: LSHA / automata learning ---
numpy==1.23.5
scipy==1.12.0
pandas==2.2.0
scikit-learn==1.4.0
imbalanced-learn==0.12.0
imblearn==0.0
tensorflow==2.12.0
z3-solver==4.12.5.0
pygad==3.5.0
tqdm==4.66.2
tabulate==0.9.0

# --- Graph / knowledge-graph ---
neo4j==5.17.0
promg==0.1.40
graphviz==0.20.1
pygraphviz==1.14           # needs system Graphviz dev headers (see migration notes)

# --- Plotting / reporting ---
matplotlib==3.8.0
reportlab==4.4.10
pillow==10.2.0

# --- Misc ---
requests==2.31.0
PyYAML==6.0.1
